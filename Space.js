// const spaces = N;
// const occupied = C;
// const unoccupied = U;
// const lots = [
//     "1st Parking Lot",
//     "2nd Parking Lot"
// ];

// 1. Create a variable for the number of parking spaces (N).
// 2. Create a variable for the 1st parking lot
// 3. Create a variable for the 2nd parking lot
// 4. For whatever N is inputed, the number of parking spaces of each lot is that number
// 5. If a spot is ocuppied (C) on the top and bottom, log that as 1.
// 6. If a spot is occupied on one, but not the other, don't log anything.
// 7. Else, don't log anything.
// 8. Afterwards, add up all of the numbers and log the answer.

// function getSpaces(spaces, yes, today) {
//   let count = 0;

//   for (let i = 0; i < spaces; i++) {
//     if (yes[i] === "C" && today[i] === "C") {
//       count++;
//     }
//   }
//   return count;
// }

// console.log(getSpaces(5, "CC..C", ".CCCC"));
// console.log(name[1]); // Call upon each character of the string

// **Pseudocode**
// Check each position and assign it a number
// Multiply the value in the position by 2^its number
// Add all of the numbers together
// Return the final result

function binarytodecimal(input) {
  let decimal = 0;

    for (let i = 0; i < input.length; i++) {
      if (input[input.length - 1 - i] ==="1"){
        decimal = decimal + Math.power(2,i)
      }
    }
      return decimal;
  }

console.log(binarytodecimal(101));

function dtob (d){
  let b = 0;

  for(let i = 0; i < ; i++)
    if (d%2 ==="0"){
      [b.] 
    }
    else (d%2 ==="1"){
      
    }
}




function binarytodecimal(one,two,) {
  let count = 0;
  for (let i = 0; i < 8; i++) {
    return ; // return the position of each space
  }

  if 
}
